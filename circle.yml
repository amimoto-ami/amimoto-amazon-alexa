---
machine:
  python:
    version: 2.7.10
dependencies:
  pre:
    - |
      virtualenv .venv
      source .venv/bin/activate
      pip install -r requirements.txt
test:
  override:
    - |
      source .venv/bin/activate
      flake8 lambda_function.py
      lamvery build

