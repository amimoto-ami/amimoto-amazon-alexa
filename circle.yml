---
machine:
  python:
    version: 2.7.10
dependencies:
  pre:
    - |
      pip install lamvery flake8
      virtualenv .venv
      source .venv/bin/activate
      pip install -r requirements.txt
test:
  override:
    - |
      flake8 lambda_function.py
      lamvery build

